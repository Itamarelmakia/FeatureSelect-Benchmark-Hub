# Data Directory

The full benchmark data (~2 GB) is **not stored in git** to keep the repository lightweight.

## How to get the full data

1. Make sure you have Python 3 installed.
2. Run the following script from the project root:

```bash
python scripts/download_data.py
